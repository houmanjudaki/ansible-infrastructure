---
kubeconfig_path: /var/lib/rancher/k3s/server/cred/admin.kubeconfig
inline_options: ""
name: my-deployment
chart: my-chart

timeout: 900

helm_version: v2

values_file:
# |
#  test_value: dummy

values_url:

repos:
#  - { name: testuser1, url: "http://....." }
